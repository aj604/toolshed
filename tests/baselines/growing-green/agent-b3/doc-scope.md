# Doc scope record
<!-- format: doc-lifecycle growing-docs -->

## Deferred
- runbook: api/worker failure triage beyond the exit codes listed in AGENTS.md — promote when: an incident or on-call question needs step-by-step recovery
- reference: HTTP API endpoint doc (`/health`, `POST /tasks`) — promote when: an external consumer asks for the contract a second time (readable today in `services/api/server.js`)
- reference: `@taskflow/shared` helper doc — promote when: a helper's contract is re-derived across sessions (readable today in `packages/shared/index.js`)
- docs/reference/ tree (per bootstrapping-docs repo-shape.md) — promote when: the repo grows beyond the current three units

## Done
- 2026-07-03 AGENTS.md (+ CLAUDE.md pointer) ← bootstrap: undocumented repo
- 2026-07-03 README.md ← bootstrap: undocumented repo
