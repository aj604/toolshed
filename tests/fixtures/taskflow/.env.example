# Copy to .env and fill in. The api reads these at startup.
# DATABASE_URL is required; the api exits 1 if it is unset.
DATABASE_URL=postgres://localhost:5432/taskflow
# Optional; defaults to 8080.
PORT=8080
