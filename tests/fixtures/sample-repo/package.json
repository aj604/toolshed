{
  "name": "bundlewatch",
  "version": "0.3.1",
  "description": "Fail CI when a build artifact exceeds its gzipped size budget.",
  "bin": {
    "bundlewatch": "./bin/cli.js"
  },
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --test test/",
    "lint": "node scripts/build.js --lint-only"
  },
  "license": "MIT"
}
